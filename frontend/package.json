{
    "name": "imago-frontend",
    "version": "1.0.0",
    "description": "Frontend for IMAGO Media Search",
    "scripts": {
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview",
      "dev:all": "concurrently \"poetry run uvicorn backend.app:app --reload --port 8000\" \"npm run dev\""
    },
    "dependencies": {
      "react": "^18.2.0",
      "react-dom": "^18.2.0"
    },
    "devDependencies": {
      "@vitejs/plugin-react": "^4.0.3",
      "concurrently": "^8.2.2",
      "vite": "^5.2.0"
    }
  }  